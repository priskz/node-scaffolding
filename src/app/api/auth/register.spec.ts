import { expect } from 'chai'
import { AxiosResponse } from 'axios'
import { appRequest } from '~/test/util'

//----- Tests -----//

describe.skip('api/auth/register', () => {
	describe('when POST request made', () => {
		it('should return 200', async () => {
			// Test
		})
	})
})
