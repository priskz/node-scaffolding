import moment from 'moment'
import { AxiosResponse } from 'axios'

//----- Tests -----//

describe('middleware/exception', () => {
	describe.skip('when an error is thrown', () => {
		it('should log thee rror', async () => {
			//
		})
	})
})
